---
interface Props {
  lang: 'tr' | 'en';
}
const { lang } = Astro.props;
const placeholder = lang === 'tr' ? 'Rehber, yazıcı modeli veya konu ara... (ESC ile kapat)' : 'Search guides, printer models, protocols... (ESC to close)';
const quickLinksTitle = lang === 'tr' ? 'Popüler Konular' : 'Popular Topics';
---

<!-- Search Modal Trigger Button -->
<button
  id="search-trigger-btn"
  type="button"
  class="flex items-center gap-2 bg-gray-100 hover:bg-gray-200 text-ink/60 hover:text-ink text-xs font-medium px-3 py-1.5 rounded-md border border-gray-300 transition"
  aria-label={lang === 'tr' ? 'Rehber ve Yazıcı Ara' : 'Search Guides'}
>
  <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="11" cy="11" r="8"></circle>
    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
  </svg>
  <span>{lang === 'tr' ? 'Ara...' : 'Search...'}</span>
  <kbd class="hidden md:inline-block bg-paper text-ink/40 font-mono text-[10px] px-1.5 py-0.5 rounded border border-gray-300">⌘K</kbd>
</button>

<!-- Backdrop & Modal -->
<div
  id="search-modal"
  class="fixed inset-0 z-50 bg-black/50 backdrop-blur-xs flex items-start justify-center p-4 pt-16 md:pt-24 hidden"
  role="dialog"
  aria-modal="true"
>
  <div class="bg-paper w-full max-w-2xl rounded-lg shadow-2xl border border-gray-200 overflow-hidden flex flex-col max-h-[80vh] animate-in fade-in zoom-in-95 duration-150">
    <!-- Search Bar Header -->
    <div class="flex items-center px-4 border-b border-gray-200 gap-3 py-3">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-accent shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="11" cy="11" r="8"></circle>
        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
      </svg>
      <input
        id="search-input"
        type="text"
        placeholder={placeholder}
        class="w-full bg-transparent border-none outline-none text-ink placeholder-ink/40 text-sm md:text-base py-1"
        autocomplete="off"
      />
      <button id="search-close-btn" class="text-ink/40 hover:text-ink p-1 rounded-md hover:bg-gray-100 transition">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>

    <!-- Quick Tags / Results Container -->
    <div id="search-results-container" class="overflow-y-auto p-4 flex-1 divide-y divide-gray-100">
      <!-- Default Quick Suggestions -->
      <div id="search-default-view">
        <p class="font-mono text-xs font-medium uppercase tracking-wider text-ink/40 mb-3">{quickLinksTitle}</p>
        <div class="flex flex-wrap gap-2 mb-6">
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="Bluetooth">Bluetooth</button>
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="ESC/POS">ESC/POS</button>
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="Zebra ZPL">Zebra ZPL</button>
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="Epson">Epson</button>
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="Xprinter">Xprinter</button>
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="Sessiz">Sessiz Yazdırma</button>
          <button type="button" class="quick-tag bg-gray-100 hover:bg-accent/10 hover:text-accent text-ink/70 text-xs px-3 py-1.5 rounded-md transition" data-query="WooCommerce">WooCommerce</button>
        </div>

        <!-- Android app: a compact secondary link, not a promo banner -->
        <a
          href="https://play.google.com/store/apps/details?id=com.mobileprint.service"
          target="_blank"
          rel="noopener"
          class="flex items-center justify-between gap-4 border border-gray-200 hover:border-accent/40 rounded-md px-4 py-3 text-sm text-ink/70 hover:text-ink transition"
        >
          <span>{lang === 'tr' ? 'Android uygulamasını mı arıyorsun?' : 'Looking for the Android app?'}</span>
          <span class="font-mono text-xs text-accent shrink-0">Google Play →</span>
        </a>
      </div>

      <!-- Live Search Results List -->
      <div id="search-dynamic-results" class="hidden space-y-2"></div>
      <div id="search-no-results" class="hidden text-center py-8 text-ink/40 text-sm">
        {lang === 'tr' ? 'Eşleşen rehber veya model bulunamadı.' : 'No matching guide or hardware found.'}
      </div>
    </div>

    <!-- Modal Footer -->
    <div class="px-4 py-2.5 bg-gray-50 border-t border-gray-200 text-[11px] text-ink/40 flex items-center justify-between font-mono">
      <span>{lang === 'tr' ? 'Seçmek için tıkla' : 'Click to navigate'}</span>
      <span>Printzen Search Engine</span>
    </div>
  </div>
</div>

<script is:inline define:vars={{ lang }}>
  let searchIndex = null;

  async function loadSearchIndex() {
    if (searchIndex) return searchIndex;
    try {
      const res = await fetch('/search-index.json');
      const data = await res.json();
      searchIndex = data[lang] || [];
      return searchIndex;
    } catch (e) {
      console.error('Search index failed to load', e);
      return [];
    }
  }

  const modal = document.getElementById('search-modal');
  const triggerBtn = document.getElementById('search-trigger-btn');
  const closeBtn = document.getElementById('search-close-btn');
  const input = document.getElementById('search-input');
  const defaultView = document.getElementById('search-default-view');
  const dynamicResults = document.getElementById('search-dynamic-results');
  const noResults = document.getElementById('search-no-results');

  function openSearch() {
    modal?.classList.remove('hidden');
    input?.focus();
    loadSearchIndex();
  }

  function closeSearch() {
    modal?.classList.add('hidden');
    if (input) input.value = '';
    showDefaultView();
  }

  function showDefaultView() {
    defaultView?.classList.remove('hidden');
    dynamicResults?.classList.add('hidden');
    noResults?.classList.add('hidden');
  }

  triggerBtn?.addEventListener('click', openSearch);
  closeBtn?.addEventListener('click', closeSearch);

  modal?.addEventListener('click', (e) => {
    if (e.target === modal) closeSearch();
  });

  window.addEventListener('keydown', (e) => {
    if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') {
      e.preventDefault();
      if (modal?.classList.contains('hidden')) openSearch();
      else closeSearch();
    }
    if (e.key === 'Escape' && !modal?.classList.contains('hidden')) {
      closeSearch();
    }
  });

  // Quick Tags click
  document.querySelectorAll('.quick-tag').forEach((btn) => {
    btn.addEventListener('click', () => {
      const q = btn.getAttribute('data-query') || '';
      if (input) {
        input.value = q;
        input.dispatchEvent(new Event('input'));
      }
    });
  });

  input?.addEventListener('input', async (e) => {
    const q = e.target.value.trim().toLowerCase();
    if (!q) {
      showDefaultView();
      return;
    }

    const items = await loadSearchIndex();
    const matches = items.filter((item) => {
      return item.title.toLowerCase().includes(q) || item.desc.toLowerCase().includes(q);
    });

    defaultView?.classList.add('hidden');

    if (matches.length === 0) {
      dynamicResults?.classList.add('hidden');
      noResults?.classList.remove('hidden');
      return;
    }

    noResults?.classList.add('hidden');
    if (dynamicResults) {
      dynamicResults.innerHTML = matches.slice(0, 8).map((m) => `
        <a href="${m.url}" class="flex items-start gap-3 p-3 rounded-md hover:bg-accent/5 group transition border border-transparent hover:border-accent/20">
          <div class="font-mono text-xs p-2 bg-gray-100 text-ink/50 rounded-md shrink-0 group-hover:bg-accent group-hover:text-paper transition">&gt;_</div>
          <div class="min-w-0">
            <h4 class="text-sm font-semibold text-ink group-hover:text-accent transition truncate">${m.title}</h4>
            <p class="text-xs text-ink/50 line-clamp-1 mt-0.5">${m.desc}</p>
          </div>
        </a>
      `).join('');
      dynamicResults.classList.remove('hidden');
    }
  });
</script>
